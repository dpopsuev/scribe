tasks:
  build:
    command: "go build -o ./bin/scribe ./cmd/scribe"

  test:
    command: "go test -json ./..."
    source: go-test
    depends: [build]

  vet:
    command: "go vet ./..."
    source: go-vet

  check:
    depends: [test, vet]
